@model List<SosyalMedya>

<div class="d-flex justify-content-center mb-5">
    @foreach (var item in Model)
    {
            <a class="btn btn-outline-primary mr-2" href="@item.Url"><i class="@item.PlatformIcon"></i></a>
    }
</div>